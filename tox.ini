[tox]
isolated_build = true
envlist = py311

[testenv]
passenv = *
deps =
    uv
allowlist_externals =
    uv
    uvx
    sphinx-build
commands =
    uv sync --dev
    uv run pytest --cov-report=xml --cov=plink_pipelines --cov-fail-under 75 tests/
    uv run ruff check .
    uvx ty check src/plink_pipelines